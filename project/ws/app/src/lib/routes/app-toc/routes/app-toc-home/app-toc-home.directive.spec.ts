import { AppTocHomeDirective } from './app-toc-home.directive'

describe('AppTocHomeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTocHomeDirective()
    expect(directive).toBeTruthy()
  })
})
