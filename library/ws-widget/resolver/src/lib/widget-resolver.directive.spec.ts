import { WidgetResolverDirective } from './widget-resolver.directive'

describe('WidgetResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetResolverDirective()
    expect(directive).toBeTruthy()
  })
})
