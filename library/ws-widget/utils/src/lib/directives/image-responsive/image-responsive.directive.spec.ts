import { ImageResponsiveDirective } from './image-responsive.directive'

describe('ImageResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageResponsiveDirective()
    expect(directive).toBeTruthy()
  })
})
