import { DefaultThumbnailDirective } from './default-thumbnail.directive'

describe('DefaultThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultThumbnailDirective()
    expect(directive).toBeTruthy()
  })
})
